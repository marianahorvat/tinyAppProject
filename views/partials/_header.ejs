<h1>TinyApp</h1>

<% if(user) { %>
  <form method="POST" action="/logout">
      <h5>Logged in as: </a><%= user.email %></h5>
      <button type="submit" class="btn-primary">Logout</button>
  </form>
<% } else { %>
      <a href= "/login">Login</a>
      <a href= "/register">Register</a>
<% } %>

  
